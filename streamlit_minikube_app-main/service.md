this file is encharge of:
    1: connecting between pods to comunicate
    2: because that the pod`s ip can change every time it restarts so the service give the pod a stable   ip and can access the pod with its name because it is have internal dns